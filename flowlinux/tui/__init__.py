"""Terminal UI — Rich + questionary (M5). No GUI.

Houses the styled installer/first-run wizard (banner, colors, spinners, model-download
progress bars), the `flowlinux config` TOML editor, the diagnostics self-test view, and
`flowlinux status`. A graphical window was rejected (ADR-0007) in favor of a terminal-first
UX. Stub until M5.
"""
