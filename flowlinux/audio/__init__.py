"""Audio capture + VAD (M2/M3).

Planned: sounddevice 16kHz mono capture, RMS level meter, Silero VAD gating, and an
optional pre-model denoise (noisereduce / RNNoise) benchmarked in M3. Stub until M2.
"""
