"""Formatting layer (M4).

Tiered + pluggable: Tier-1 rule-based (filler removal, punctuation, casing, custom
dictionary) as the always-on default; Tier-2 cloud LLM (opt-in); Tier-3 local LLM
(opt-in, OFF on this memory-constrained machine). Stub until M4.
"""
