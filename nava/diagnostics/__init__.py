"""Diagnostics / health checks surfaced in the UI (M5).

For M1, health checks live on each Injector and are exposed via `nava-inject
--doctor`. This package will aggregate all subsystem health (X11, xdotool, clipboard,
GPU/CUDA, mic) for the settings diagnostics panel. Stub until M5.
"""
